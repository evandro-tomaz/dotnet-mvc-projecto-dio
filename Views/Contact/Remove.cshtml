@model ProjetoMVC.Models.Contact

@{
    ViewData["Title"] = "Excluir Contato";
}

<h1>Você realmente quer apagar este contato?</h1>
<p>Essa operação é <strong>IRREVERSÍVEL</strong></p>
<hr />

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Name)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Name)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.PhoneNumber)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.PhoneNumber)
        </dt>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(x => x.Active)
        </dt>
        <dt class="col-sm-10">
            @Html.DisplayFor(x => x.Active)
        </dt>
    </dl>
</div>
<div class="col-md-4">
    <form asp-action="Remove">
        <div class="form-group">
            <input type="submit" value="Deletar" class="btn btn-danger" />
            <a asp-action="Index" class="btn btn-primary">Voltar</a>
        </div>
    </form>
</div>